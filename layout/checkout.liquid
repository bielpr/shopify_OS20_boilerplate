{% comment %} Optimized Checkout for Better UX and Trust Signals {% endcomment %}

<!DOCTYPE html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Finalizar Compra - {{ shop.name }}</title>
  {{ content_for_header }}
  <style>
    .checkout-section {
      padding: 40px;
      background: #f9f9f9;
    }
    .checkout-summary {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
    .checkout-item {
      display: flex;
      align-items: center;
      gap: 15px;
    }
    .checkout-item img {
      width: 60px;
      border-radius: 5px;
    }
    .checkout-item-details p {
      margin: 0;
    }
    .checkout-total {
      margin-top: 20px;
      font-size: 18px;
    }
    .checkout-actions {
      margin-top: 20px;
      display: flex;
      gap: 15px;
    }
    .btn-primary {
      background: #e76f51;
      color: #fff;
      padding: 10px 20px;
      border-radius: 5px;
      font-size: 18px;
      text-decoration: none;
    }
    .btn-secondary {
      background: #555;
      color: #fff;
      padding: 10px 20px;
      border-radius: 5px;
      font-size: 18px;
      text-decoration: none;
    }
  </style>
</head>
<body>
  {{ content_for_layout }}
  <section class="checkout-section">
    <div class="container">
      <h1>Finalizar Compra</h1>
      <p>Revisa tu pedido y completa la informaciÃ³n de pago.</p>
      
      <div class="checkout-summary">
        {% for item in cart.items %}
          <div class="checkout-item">
            <img src="{{ item.product.featured_image | img_url: 'small' }}" alt="{{ item.product.title }}">
            <div class="checkout-item-details">
              <p><strong>{{ item.product.title }}</strong></p>
              <p>Cantidad: {{ item.quantity }}</p>
              <p>Precio: {{ item.line_price | money }}</p>
            </div>
          </div>
        {% endfor %}
      </div>
      
      <div class="checkout-total">
        <p><strong>Total: {{ cart.total_price | money }}</strong></p>
      </div>
      
      <div class="checkout-actions">
        <a href="/cart" class="btn btn-secondary">Modificar Pedido</a>
        <a href="/checkout" class="btn btn-primary">Proceder al Pago</a>
      </div>
    </div>
  </section>
</body>
</html>